version: "3.8"

services:
  postgres_ka_container:
    image: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_DB: mydb
      POSTGRES_PASSWORD: password

  redis:
    image: redis
    ports:
      - "6379:6379"